code=input("ENter code")
guess=input("Enter guess")
hits=0
nhits=0;
for i in range(len(code)):
        if code[i]==guess[i]:
                hits+=1
print(hits)
for i in range(len(code)):
        for j in range(len(guess)):
                if code[i]==guess[j]:
                        nhits+=1
                        break
print(nhits-hits)
